# Read the Docs build config — MARC 2026 Developer Guide (DRAFT)
# Ported from the 2025 guide (ubuntu-22.04 / python 3.10). RTD slug: marc-challenge.
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - requirements: requirements.txt

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false
  builder: html

# Bilingual note (draft): docs/ (en) is the primary build. The Korean build
# (docs_ko/) is published as a separate RTD language project or a second config,
# matching the 2025 folder-split pattern. Decide single-project+toggle vs.
# per-language project before promoting out of draft/.

# formats:
#   - pdf
#   - epub
